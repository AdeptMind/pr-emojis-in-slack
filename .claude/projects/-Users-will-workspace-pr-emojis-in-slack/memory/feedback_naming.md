---
name: No slapr naming
description: The word "slapr" is offensive and must not be used anywhere in the new project — use "pr-emojis-in-slack" instead
type: feedback
---

Never use the name "slapr" in the Go rewrite. The project name is `pr-emojis-in-slack`.

**Why:** The user considers "slapr" offensive. The Python reference uses it but the Go rewrite must not.
**How to apply:** Use `pr-emojis-in-slack` for the binary name, command directory, Docker image references, action name, env var prefixes, and all user-facing strings. Only reference "slapr" when reading the Python source as a transcription reference.
